import { useState } from "react";

const SearchBar = () => {
  return (
    <div>
      <form>
        <input placeholder="Search"/>
        <button>Search</button>
      </form>
    </div>
  );
};

export default SearchBar;
